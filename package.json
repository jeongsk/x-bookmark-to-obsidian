{
  "name": "x-bookmark-to-obsidian",
  "version": "2.5.0",
  "private": true,
  "description": "Chrome Extension: Save X bookmarks to Obsidian",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
